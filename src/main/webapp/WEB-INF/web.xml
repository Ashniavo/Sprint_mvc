<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Packages a scanner (separateur configurable) -->
    <context-param>
        <param-name>package_list</param-name>
        <param-value>sharon.app.controllers</param-value>
    </context-param>

    <context-param>
        <param-name>list_separator</param-name>
        <param-value>;</param-value>
    </context-param>

    <!-- Listener -->
    <listener>
        <listener-class>sharon.sprintmvc.listener.AppServletContextListener</listener-class>
    </listener>

    <servlet>
        <servlet-name>frontController</servlet-name>
        <servlet-class>sharon.sprintmvc.controllers.FrontControllerServlet</servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>frontController</servlet-name>
        <url-pattern>/*</url-pattern>
    </servlet-mapping>

</web-app>